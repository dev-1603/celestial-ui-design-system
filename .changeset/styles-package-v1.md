---
"@celestial-ui/styles": minor
---

Add `@celestial-ui/styles` V1 — framework-neutral CSS delivery layer over `ResolvedTheme`.

- Deterministic compiler with `--cui-*` compatibility variables and versioned semantic CSS API
- Document/application/sandbox scope model with `@layer celestial.tokens`
- Idempotent CSP-aware runtime style manager
- SSR helpers with system-mode no-flash bootstrap
- Optional Tailwind v4 `@theme inline` bridge and shadcn variable adapter
- Opt-in minimal base/accessibility CSS
