import { getGeneratedComponentSpec } from './spec-lookup';

export const centerSpec = getGeneratedComponentSpec({
  id: 'center',
  displayName: 'Center',
  purpose: 'Centers content along one or both axes.',
  taxonomy: 'atomic',
  engineeringFamily: 'layout',
  complexity: 'simple',
  status: 'stable',
  profile: 'layout',
});
