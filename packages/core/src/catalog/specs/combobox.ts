import { getGeneratedComponentSpec } from './spec-lookup';

export const comboboxSpec = getGeneratedComponentSpec({
  id: 'combobox',
  displayName: 'Combobox',
  purpose: 'Filterable selection from editable input.',
  taxonomy: 'molecular',
  engineeringFamily: 'forms',
  complexity: 'complex',
  status: 'stable',
  profile: 'form-selection',
});
