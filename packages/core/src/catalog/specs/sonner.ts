import { getGeneratedComponentSpec } from './spec-lookup';

export const sonnerSpec = getGeneratedComponentSpec({
  id: 'sonner',
  displayName: 'Sonner',
  purpose: 'Stacked toast notification host pattern.',
  taxonomy: 'molecular',
  engineeringFamily: 'feedback',
  complexity: 'moderate',
  status: 'stable',
  profile: 'feedback',
});
