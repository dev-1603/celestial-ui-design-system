import { getGeneratedComponentSpec } from './spec-lookup';

export const toggleGroupSpec = getGeneratedComponentSpec({
  id: 'toggle-group',
  displayName: 'Toggle Group',
  purpose: 'Grouped toggle buttons with shared selection.',
  taxonomy: 'molecular',
  engineeringFamily: 'forms',
  complexity: 'moderate',
  status: 'stable',
  profile: 'form-binary',
});
